service:
  name: grifor-serverless-app
# app and org for use with dashboard.serverless.com
#app: your-app-name
#org: your-org-name

custom:
  webpack:
    webpackConfig: ./webpack.config.js
    includeModules: true

# Add the serverless-webpack plugin
plugins:
  - serverless-webpack
  - serverless-iam-roles-per-function
  - serverless-offline
  - serverless-dynamodb-local

provider:
  name: aws
  runtime: nodejs12.x

  stage: ${opt:stage, 'dev'}
  region: ${opt:region, 'us-east-2'}

  apiGateway:
    minimumCompressionSize: 1024 # Enable gzip compression for responses > 1 KB
  environment:
    AWS_NODEJS_CONNECTION_REUSE_ENABLED: 1
    WORK_CALENDAR_TABLE: WorkCalendarTable-${self:provider.stage}

functions:
  GetWorkCalendar:
    handler: src/http/lambda/getWorkCalendar.handler
    events:
      - http:
          method: get
          path: /calendar/{year}/{month}
          cors: true

  CreateWorkCalendar:
    handler: src/http/lambda/createWorkCalendarhandler
    events:
      - http:
          method: patch
          path: /calendar/{year}/{month}
          cors: true

  UpdateWorkCalendar:
    handler: src/http/lambda/updateWorkCalendar.handler
    events:
      - http:
          method: patch
          path: /calendar/{year}/{month}
          cors: true

  DeleteWorkCalendar:
    handler: src/http/lambda/deleteWorkCalendar.handler
    events:
      - http:
          method: delete
          path: /calendar/{year}/{month}
          cors: true

resources:
  # DynamoDB
  - ${file(resources/dynamodb.yml)}
  # Cognito
  - ${file(resources/cognito.yml)}

  Resources:
    GatewayResponseDefault4XX:
      Type: AWS::ApiGateway::GatewayResponse
      Properties:
        ResponseParameters:
          gatewayresponse.header.Access-Control-Allow-Origin: "'*'"
          gatewayresponse.header.Access-Control-Allow-Headers: "'Content-Type,X-Amz-Date,Authorization,X-Api-Key,X-Amz-Security-Token'"
          gatewayresponse.header.Access-Control-Allow-Methods: "'GET,OPTIONS,POST'"
        ResponseType: DEFAULT_4XX
        RestApiId:
          Ref: ApiGatewayRestApi

